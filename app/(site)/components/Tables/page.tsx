import type { Metadata } from "next";
import ComingSoon from "../_shared/ComingSoon";

export const metadata: Metadata = {
  title: "Tables — Aura UI",
};

export default function TablesCategoryPage() {
  return <ComingSoon slug="Tables" />;
}
