import type { Metadata } from "next";
import ComingSoon from "../_shared/ComingSoon";

export const metadata: Metadata = {
  title: "Inputs — Aura UI",
};

export default function InputCategoryPage() {
  return <ComingSoon slug="Input" />;
}
