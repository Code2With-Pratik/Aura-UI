import type { Metadata } from "next";
import ComingSoon from "../_shared/ComingSoon";

export const metadata: Metadata = {
  title: "Page Transition — Aura UI",
};

export default function PageTransitionCategoryPage() {
  return <ComingSoon slug="PageTransition" />;
}
