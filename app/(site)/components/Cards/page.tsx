import type { Metadata } from "next";
import ComingSoon from "../_shared/ComingSoon";

export const metadata: Metadata = {
  title: "Cards — Aura UI",
};

export default function CardsCategoryPage() {
  return <ComingSoon slug="Cards" />;
}
