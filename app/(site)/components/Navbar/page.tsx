import type { Metadata } from "next";
import ComingSoon from "../_shared/ComingSoon";

export const metadata: Metadata = {
  title: "Navbar — Aura UI",
};

export default function NavbarCategoryPage() {
  return <ComingSoon slug="Navbar" />;
}
