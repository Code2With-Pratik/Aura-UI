import type { Metadata } from "next";
import ComingSoon from "../_shared/ComingSoon";

export const metadata: Metadata = {
  title: "Dock — Aura UI",
};

export default function DockCategoryPage() {
  return <ComingSoon slug="Dock" />;
}
