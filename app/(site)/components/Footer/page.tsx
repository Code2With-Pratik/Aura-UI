import type { Metadata } from "next";
import ComingSoon from "../_shared/ComingSoon";

export const metadata: Metadata = {
  title: "Footer — Aura UI",
};

export default function FooterCategoryPage() {
  return <ComingSoon slug="Footer" />;
}
