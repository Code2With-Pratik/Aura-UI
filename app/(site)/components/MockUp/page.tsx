import type { Metadata } from "next";
import ComingSoon from "../_shared/ComingSoon";

export const metadata: Metadata = {
  title: "Mockup — Aura UI",
};

export default function MockUpCategoryPage() {
  return <ComingSoon slug="MockUp" />;
}
